<h1>"Kingdoms"</h1>
{{#each kingdoms as |kingdom|}}
  <div class="kingdom">
  <div class="ID">{{kingdom.id}}</div>
  <div class="Name">{{kingdom.name}}</div>
</div>
{{/each}}

<h2>Make a new kingdom</h2>
<form method="post" action="/kingdoms">
  <label>Name:</label><input type="text" name="name">
  <input type="submit">
</form>
