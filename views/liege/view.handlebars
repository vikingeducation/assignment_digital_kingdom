<div class="row">
  <div class="col-sm-6">
    <h3>Vassals for {{data.name}}</h3>
  </div>
</div>
{{#each data.Vassals as |vassal|}}
<div class="row">
  <div class="col-sm-6">
      {{vassal.name}}
  </div>
</div>
{{/each}}
<div class="row">
  <div class="col-sm-6">
---Add Vassal---
  </div>
</div>
    <form action="/kingdoms/{{kingdomId}}/castles/{{castleId}}/lieges/{{liegeId}}/vassals" method="POST">

  <div class="row">
    <div class="col-sm-6">
  <input type="text" name="vassalName" id="name" placeholder="Vassal">
</div>
</div>

      <div class="row">
        <div class="col-sm-6">
      <input type="submit" class="btn btn-success" value="Add New Vassal" method="post">
    </div>
  </div>
    </form>
