<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Castle</title>
  </head>
  <body>

    <div class='header'>
      <h1>Castle View</h1>
    </div>

    <div class='box'>
      <h3 id='castle-name'>{{castleName}}</h3>

      {{#each lieges as |liege|}}
        <p>{{liege}}</p>
      {{/each}}

      <p># of vassals: {{vassals}}</p>
    </div>

    <div class='form'>
      <h3>Add a new liege</h3>
      <form action='/kingdoms/{{kingdomId}}/castles/{{castleId}}/edit' method='POST'>
        <input type='text' placeholder='Enter a liege name' name='liege'>
        <button type='submit'>Add liege</button>
      </form>
    </div>

  </body>
</html>
